# Audio FingerPrinting SmallCNN

Lightweight CNN experiments for audio fingerprinting; download `fma_small` separately (git-ignored) before running preprocessing and training.

![System overview](System_overview.png)

